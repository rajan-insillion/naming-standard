{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://example.com/common_types.json",
    "title": "Common Data Types for Insurance Application",
    "description": "Standardized data types used across various insurance application schemas.",
    "definitions": {
        "date": {
            "type": "string",
            "format": "date",
            "description": "A date in 'YYYY-MM-DD' format."
        },
        "currency": {
            "type": "number",
            "format": "currency",
            "description": "A monetary value, typically with 2 decimal places.",
            "minimum": 0
        },
        "phone_no": {
            "type": "string",
            "pattern": "^\\+?[0-9\\s\\-()]{7,25}$",
            "description": "A phone number, optionally starting with a '+' for international codes."
        },
        "email": {
            "type": "string",
            "format": "email",
            "description": "A standard email address."
        },
        "website": {
            "type": "string",
            "format": "uri",
            "description": "A valid URL for a website."
        },
        "country": {
            "type": "string",
            "format": "string",
            "description": "ISO 3166-1 alpha-2 country code (e.g., 'US' for United States, 'IN' for India.",
            "minimum": 0
        },
        "phone": {
            "type": "string",
            "format": "string",
            "description": "E.164 international format, ensuring all non-numeric characters except the leading '+' are removed.",
            "minimum": 0
        },
        "naic": {
            "type": "string",
            "pattern": "^[0-9]{5}$",
            "description": "Five-digit NAIC company code for US insurance carriers."
        },
        "pan": {
            "type": "string",
            "pattern": "^[A-Z]{5}[0-9]{4}[A-Z]$",
            "description": "10-digit PAN number; India specific."
        },
        "year": {
            "type": "number",
            "description": "A calendar year in YYYY format.",
            "minimum": 1900,
            "maximum": 2100
        }
    }
}
